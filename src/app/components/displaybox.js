var React = require('react')
var ReactDOM = require('react-dom')

export default class DisplayBox extends React.Component {
  constructor(props) {
    super(props)
  }

  render() {
    return (
      <div>
        Display list Box Component
      </div>
    )
  }
}